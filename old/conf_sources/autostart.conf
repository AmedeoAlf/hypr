# exec-once = ulauncher
# exec-once = dunst
exec-once = waybar
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = hyprpaper

exec = hyprshade auto
exec-once = copyq --start-server #& for i in {1..5}; do sleep 1 && copyq hide; done

# non so a cosa serva
# exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
